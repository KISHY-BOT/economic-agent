# Example environment variables for Railway
# Set to 'true' to make the worker run only once per deploy
RUN_ONCE=false
# Interval between runs in seconds (default 21600 = 6 hours)
RUNTIME_INTERVAL_SECONDS=21600
